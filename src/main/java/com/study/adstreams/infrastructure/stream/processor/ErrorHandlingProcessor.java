package com.study.adstreams.infrastructure.stream.processor;

import lombok.extern.slf4j.Slf4j;

/**
 * 에러 처리를 위한 Processor (향후 확장용)
 * 현재는 DeserializationExceptionHandler를 사용하여 에러 처리
 */
@Slf4j
public class ErrorHandlingProcessor {
    // Processor API를 사용한 에러 처리 구현 시 사용
    // 현재는 DeserializationExceptionHandler를 통해 처리
}
